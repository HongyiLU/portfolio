{
  "name": "game-design-portfolio",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "clean:dist": "node tools/clean_dist.mjs",
    "prebuild": "npm run clean:dist",
    "build": "astro build",
    "preview": "astro preview",
    "dist:audit": "node tools/audit_dist_links.mjs",
    "verify": "npm run build && npm run dist:audit"
  },
  "dependencies": {
    "astro": "^5.6.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "playwright": "^1.61.1"
  }
}
